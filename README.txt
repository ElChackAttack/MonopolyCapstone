The master branch of this platform is a slightly modified version of the Monopoly Simulator created by Marc Cork and published May 13, 2016.

To Install and run the project From Command Line:

Ensure Maven is installed on machine then from the directory containing pom.xml run.
mvn clean install
mvn jfx:run
